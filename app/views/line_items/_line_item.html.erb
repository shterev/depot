<tr>
  <td><%= link_to line_item.product.title, product_path(line_item.product.id) %></td>
  <td><%= line_item.quantity %> &times; <%= number_to_currency(line_item.product.price) %> </td>
  <td class="item_price"> = <%= number_to_currency(line_item.total_price) %></td>
  <td><%= button_to 'remove', line_item, :method => :delete %> </td>
</tr>